var app = angular.module('FacesUser', []);

app.factory('UserService', [function() {
	var UserService = // Here is where we will write the ajax code
}]);

app.service('User', function() {
	// This will be the singleton that will represent the current user
});